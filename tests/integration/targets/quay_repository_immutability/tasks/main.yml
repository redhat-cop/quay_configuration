---
# Supporting repository
- name: Ensure repository ansibletestrepo exists
  infra.quay_configuration.quay_repository:
    name: ansibletestorg/ansibletestrepo
    visibility: private
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false

- name: ERROR EXPECTED Nonexisting namespace
  infra.quay_configuration.quay_repository_immutability:
    repository: nonexisting/ansibletestrepo
    tag_pattern: "release-.*"
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false
  ignore_errors: true
  register: result

- name: Ensure that the task failed
  ansible.builtin.assert:
    that: result['failed']
    fail_msg: The preceding task should have failed (nonexisting organization)

- name: ERROR EXPECTED Nonexisting repository
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/nonexisting
    tag_pattern: "release-.*"
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false
  ignore_errors: true
  register: result

- name: Ensure that the task failed
  ansible.builtin.assert:
    that: result['failed']
    fail_msg: The preceding task should have failed (nonexisting repository)

- name: Ensure the immutability policy 1 exists
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "release-.*"
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false

- name: Ensure the immutability policy 1 exists (no change 1)
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "release-.*"
    behavior: matching_immutable
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false
  register: result

- name: Ensure that the task did not change anything
  ansible.builtin.assert:
    that: not result['changed']
    fail_msg: The preceding task should not have changed anything

- name: Ensure the immutability policy 1 exists (no change 2)
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "release-.*"
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false
  register: result

- name: Ensure that the task did not change anything
  ansible.builtin.assert:
    that: not result['changed']
    fail_msg: The preceding task should not have changed anything


- name: Ensure the immutability policy 2 exists
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "test-.*"
    behavior: not_matching_immutable
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false

- name: ERROR EXPECTED Ensure the immutability policy 1 is renamed
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "release-.*"
    new_tag_pattern: "test-.*"
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false
  ignore_errors: true
  register: result

- name: Ensure that the task failed
  ansible.builtin.assert:
    that: result['failed']
    fail_msg: The preceding task should have failed (duplicate policy)

- name: Ensure the immutability policy 1 is renamed and updated
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "release-.*"
    new_tag_pattern: "prod-.*"
    behavior: not_matching_immutable
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false

- name: Ensure the immutability policy 3 exists
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "foobar-.*"
    new_tag_pattern: "dev-.*"
    behavior: not_matching_immutable
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false

- name: Ensure the immutability policy 3 is updated (1)
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "foobar1-.*"
    new_tag_pattern: "dev-.*"
    behavior: matching_immutable
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false

- name: Ensure the immutability policy 3 is updated (2)
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "dev-.*"
    behavior: not_matching_immutable
    state: present
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false

- name: Ensure the immutability policy 1 does not exists
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "prod-.*"
    behavior: matching_immutable
    state: absent
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false

- name: Ensure the immutability policy 2 does not exists
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "test-.*"
    state: absent
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false

- name: Ensure the immutability policy 3 does not exists
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "dev-.*"
    behavior: matching_immutable
    state: absent
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false

- name: Ensure the immutability policy 1 does not exists (no change)
  infra.quay_configuration.quay_repository_immutability:
    repository: ansibletestorg/ansibletestrepo
    tag_pattern: "release-.*"
    state: absent
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false
  register: result

- name: Ensure that the task did not change anything
  ansible.builtin.assert:
    that: not result['changed']
    fail_msg: The preceding task should not have changed anything

- name: Ensure the repository is removed
  infra.quay_configuration.quay_repository:
    name: ansibletestorg/ansibletestrepo
    state: absent
    quay_host: "{{ quay_url }}"
    quay_token: "{{ quay_token }}"
    validate_certs: false
...
